---
note_type: home_context
template_version: 1
contract_version: 1
title: Active Context
status: active
created: YYYY-MM-DD
updated: '2026-08-16'
tags:
  - agent-vault
  - home
  - context
last_updated: '2026-08-16'
tests: desktop 1168 unit, runtime 439, contracts 179, harness 120 (+2 skipped) - all green on the STEP-24-05 branch; chat/projects/sessions/resume/lifecycle E2E 18 passed; full desktop test:e2e is 88 passed / 2 pre-existing environmental failures locally
coverage: pre-pivot metric; re-baseline once the Phase 23 chat surface is complete
target: suites green per step (ARCH-0009 validation rules)
blockers: none
in_flight: 'PHASE-25 is COMPLETE and fully merged (STEP-25-01 PR #32 / a87894e, 25-02 PR #33 / 0303c44, 25-03 PR #34 / d9e0ffc, 25-04 PR #35 / 258953f). PHASE-26 is refined and is the next execution target; its refinement lives on branch phase/26-refine (not yet PR''d). PHASE-30 Session Config Options was created 2026-08-15 out of that refinement and EXECUTES BETWEEN 26 AND 27 despite its id — see DEC-0021 (proposed). OWED AND NOT DONE: a manual side-by-side Pi + opencode run (PHASE-25''s one unticked acceptance criterion), a run against a real unauthenticated harness, and the manual GUI/real-Pi walkthrough carried since phases 23-24.'
phase: Phase_26_generic_harness_support_and_conformance (next execution target; PHASE-25 COMPLETE, 21-24 complete and merged)
done: PHASE-21 (teardown + foundations), PHASE-22 (@srgnt/harness ACP core + Pi spike, DEC-0018 ratified), PHASE-23 (chat UI v1 over ephemeral ACP sessions, mock-driven E2E), PHASE-24 (projects, session persistence, honest resume, derived transcript checkpointing, idle reaping, bounded quit cleanup), PHASE-25 (opencode as second harness, harness settings UI, capability matrix, auth surfacing, cross-harness lessons-learned)
remaining: phases 26, 30, 27, 28, 29 in that execution order (generic harness + conformance, session config options, groups, pipelines, release)
---

# Active Context

Keep this note short, current, and safe to overwrite as the repo focus changes.

## Current Objective

<!-- AGENT-START:current-focus -->
_Last refreshed: 2026-08-16._
- Session in progress: none.
- Current step: [[02_Phases/Phase_26_generic_harness_support_and_conformance/Steps/Step_01_build-custom-harness-editor-with-launch-specs-and-capability-overrides|STEP-26-01 Build custom harness editor with launch specs and capability overrides]] - status: planned - phase: [[02_Phases/Phase_26_generic_harness_support_and_conformance/Phase|Phase 26 generic harness support and conformance]]
- Active phase: [[02_Phases/Phase_30_session_config_options/Phase|PHASE-30 Session Config Options]] - status: planned - updated: 2026-08-15
- Also active: 27 more additional steps.
<!-- AGENT-END:current-focus -->

> **How to read the block above.** It is generated by `vault_refresh` from note metadata and surfaces the newest *planned* notes, not the execution frontier — which is why it points at STEP-26-01 and names PHASE-30 as "active". Planning pointers are not execution pointers. The authoritative execution state is the frontmatter `phase`/`in_flight` fields and the Next Actions below: **phases 21-25 are complete** and **PHASE-26 is the next execution target**.
>
> **Phase ids are allocation order, not execution order.** Execution order from here is **26 → 30 → 27 → 28 → 29**. PHASE-30 (Session Config Options) was created after 27-29 already existed, so its id is higher than its position; renumbering 27-29 was rejected because 82 vault files reference those phase paths as validated wikilinks. All three neighbouring phase notes state this. When the generated block and the frontmatter disagree, the frontmatter wins.

## Repo Snapshot

- Repository: \`srgnt\`
- Shape: pnpm workspace monorepo
- Core stack: TypeScript, Electron, React, Effect Schema, Vitest
- Primary references: [[01_Architecture/System_Overview|System Overview]] and [[01_Architecture/Code_Map|Code Map]]

## In Scope Right Now

- Keep roadmap, phase notes, and step notes aligned with the implemented repo state.
- Finish the missing base-product work in workspace persistence, runtime indexing, live-ish connector behavior, and the flagship workflow.
- Preserve the local-first and renderer-secret boundaries while later phases move from scaffolding toward real behavior.

## Out Of Scope Right Now

- Live sync backend or remote source-of-truth work.
- Premium Fred workflow expansion before the base workflow is complete without AI.
- Remote telemetry or hosted crash collection beyond the current local-only posture.

## Working Assumptions

- This repo should use exactly one vault at \`.agent-vault/\`.
- The vault should not contain a second nested project folder.
- Notes should remain easy to read in raw Markdown without Obsidian-specific features.

## Blockers

<!-- AGENT-START:blockers -->
- No phase, step, or session notes are currently marked blocked.
<!-- AGENT-END:blockers -->

## Open Questions

- (Resolved 2026-03-29) Resume from SESSION-2026-03-29-132728: STEP-07-03 implementation complete, manual validation pending
- (Resolved 2026-03-29) Desktop app builds successfully for STEP-07-03 manual validation

- (Resolved 2026-03-26) Dataview feasibility: DEC-0011 accepted - Dataview extraction not feasible, SimpleQueryEngine over CanonicalStore for v1.
- How much of Phases 09-10 should remain architecture/scaffolding versus becoming executable backlog before the base workflow is truly complete?

## Critical Bugs

<!-- AGENT-START:critical-bugs -->
- No open sev-1 or sev-2 bugs are currently recorded.
<!-- AGENT-END:critical-bugs -->

## Resume Point

[[SRGNT|Resume Point — Phase 02 Onward]] captures the immediate next steps for each phase.

## Next Actions

- **Resume point (single source of truth)**: PHASE-24 is COMPLETE and fully merged — STEP-24-01 (SessionStore, PR #26), 24-02 (projects + switcher, PR #27), 24-03 (session list, auto-titles, concurrency, PR #29), 24-04 (honest resume + fork-with-handoff, PR #30), 24-05 (derived `transcript.md` checkpointing, idle reaping, bounded quit cleanup, PR #31 / a8e4269). **PHASE-25 is COMPLETE**: STEP-25-01 (opencode as a built-in harness, runtime capability detection, display-only capability cache — PR #32 / a87894e), 25-02 (harness settings UI, per-project defaults, `harnesses.json` service with its boundary rules — PR #33 / 0303c44), 25-03 (capability matrix, `SAuthMethod` normalizer, auth panel — PR #34 / d9e0ffc), 25-04 (cross-harness lessons-learned note — PR #35 / 258953f). **Next execution target is PHASE-26**, refined 2026-08-15 on branch `phase/26-refine` (2 commits, not yet PR'd — fold into PHASE-26's first PR or open separately). Execution order from here is **26 → 30 → 27 → 28 → 29**. Still carried and unowned: promote `DEFAULT_IDLE_TIMEOUT_MS` into `settings.json`. PHASE-25's notes were re-refined 2026-08-13 (328583f) and its STEP-25-03 brief corrected 2026-08-14 (f8b8f6d).
- **Carry into PHASE-26**: read [[06_Shared_Knowledge/cross-harness-lessons-learned|Cross-Harness Lessons Learned]] before designing the custom-harness editor — 18 REQ-26-xx entries, each with an evidence pointer and an owning deliverable. PHASE-26 was refined against that list on 2026-08-15 (branch `phase/26-refine`); three findings there were corrections, not additions: the ACP registry feed **exists** (so STEP-26-03 is a real integration), `claude-code-acp` is **not** a registry entry (it is `claude-acp`), and `binary`-distribution catalog entries need download+extract, which collides with the never-installs non-goal — decided as visible-but-not-one-click-addable.
- **PROP-A now has an owner**: [[02_Phases/Phase_30_session_config_options/Phase|PHASE-30 Session Config Options]] via [[04_Decisions/DEC-0021_adopt-acp-session-config-options-and-retire-the-modes-path|DEC-0021]] (proposed — accept before executing). PHASE-26 only *reports* config options. srgnt's model/mode selector currently drives **neither** shipped harness: `readModes` reads a `modes` block that opencode never sends and pi sends only from `session/load`, while both send `configOptions`. `session/set_config_option` was measured working against opencode 1.18.18 on 2026-08-15.
- **PHASE-25's unticked acceptance criterion is real, not bookkeeping**: no manual side-by-side Pi + opencode run was ever performed, and no manual GUI pass has happened since Phase 23. Auth guidance has never been exercised against a real unauthenticated harness. These belong in a release checklist; do not read the merged PRs as covering them.
- STEP-24-05 landed the phase's two remaining ARCH-0009 invariants. `transcript.md` is a DERIVED CACHE rendered from `events.jsonl` + `meta.json` (`renderTranscript` in `@srgnt/runtime`, `SessionStore.checkpointTranscript`), checkpointed at turn end / every 30 s while a turn runs / close / quit / session open, never dual-written per token - so the 30 s cadence is a freshness bound for external readers, NOT the crash-loss bound. Idle reaping is an explicit `Supervisor.setIdleHold` disarm-on-turn-start / re-arm-on-turn-end transition (heartbeats cannot protect a silent turn); a reaped session hibernates rather than closes and the next prompt revives it through the STEP-24-04 cascade. Quit runs best-effort `session/cancel` + a final checkpoint + kill-trees under ONE 2 s deadline (`packages/desktop/src/main/chat/quit.ts`).
- STEP-23-05 shipped `packages/desktop/e2e/chat.spec.ts` (8 tests, 7 behaviors) driving the real spawned mock agent, plus three things the next engineer needs: scenarios are injected per test via `test.use({ mockScenario })` -> `SRGNT_MOCK_SCENARIO` -> `resolveMockScenarioPath()`; agent-side `expect_*` failures only escape the spawned agent through `--assertions` and the `agentAssertions` fixture; and `disableAnimations()` is unusable in chat specs because the renderer CSP rejects injected inline styles.
- `MOCK_DEMO_SCENARIO` still exists and is still the default for a manual `pnpm dev` run — the injection seam overrides it rather than replacing it, so the mode selector and slash menu remain reachable by hand.
- **Owed and NOT performed** (verification, not implementation; carried from STEP-23-04, STEP-23-05 and now STEP-24-05): a manual `pnpm dev` walkthrough against the mock, a manual real-Pi run (mode switch, cancel a long turn, externally kill `pi-acp` to confirm the crash banner and recovery, PLUS letting a session idle past the 10-minute timeout to see the reap in `ps` and a transparent respawn, and quitting mid-turn to see `ps` clean), and confirmation that the Desktop E2E GitHub Actions workflow is green with `chat.spec.ts` and the new `lifecycle.spec.ts` in it. Every idle-reaping and quit-cleanup claim currently rests on mock-agent E2E and unit tests.
- `packages/desktop/e2e/lifecycle.spec.ts` skips its process-tree assertion on Windows (`ps` is POSIX). If desktop E2E ever runs on windows-latest, a `tasklist` equivalent is the gap.
- Known pre-existing local E2E failures, unrelated to Phase 23: `app.spec.ts > exercises preload APIs...` (`posix_spawnp failed` — node-pty cannot spawn on this machine) and `bug-0013-visual.spec.ts` (requires a Linux packaged build).
- Do not treat STEP-26-01 or PHASE-29 as work in progress. They appear in the generated Current Objective block because they are the newest planned notes, not because anything is executing there.
- Keep phase and home notes honest whenever implementation status changes.
