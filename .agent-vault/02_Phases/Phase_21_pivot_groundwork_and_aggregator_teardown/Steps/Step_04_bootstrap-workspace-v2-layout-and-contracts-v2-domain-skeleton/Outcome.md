# Outcome

- Result: complete (2026-07-12). Workspace bootstrap now creates the v2 layout — `projects/`, `groups/templates/`, seeded `harnesses.json` (`{"version":1,"harnesses":[]}`) and `settings.json` (`{}`) — in both `packages/runtime/src/workspace/bootstrap.ts` and desktop main's `ensureWorkspaceLayout`; bootstrap is strictly additive (seed writes use `wx`, v1/PARA dirs ignored, never removed) and idempotent. Contracts v2 skeleton landed: `project.ts` (SProject), `session.ts` (SSession with kind single|group, status active|idle|interrupted|error|closed, parentSessionId; SSessionEvent envelope `{seq, ts, protocolVersion, kind, payload}` with tolerant open-kind reader `readSessionEvent`), `harness.ts` (SHarnessDefinition, SLaunchSpec, quirks enum, capability overrides, SHarnessesFile). Aggregator entity/skills schemas deleted (launch.ts kept — still consumed). Repo migrated off deprecated `@effect/schema` to core `effect` Schema; dependency removed from all three package.json files and the lockfile.
- Validation: contracts 127/127 (incl. fast-check tolerant-reader properties + Pi definition round-trip); runtime 287/287 (incl. rewritten v2 bootstrap tests: exact layout, idempotency, never-overwrite, v1 dirs untouched, read-only validate with missingFiles); desktop 758/758; `pnpm typecheck` clean; `rg '@effect/schema' packages/ pnpm-lock.yaml` → zero. Desktop e2e 68 passed / 3 failed — all three are documented pre-existing baselines (app.spec PTY posix_spawnp, bug-0013 Linux-only, gfm-compliance .cm-header-*); the settings-persistence e2e covering the new `settings.json` path passed and onboarding was walked by e2e against the v2 layout.
- Follow-up: (1) PHASE-24 should unify desktop main's `ensureWorkspaceLayout` onto runtime `bootstrapWorkspace` when SessionStore/project store land; (2) terminal run logs still write to `.command-center/runs/` — dies with the aggregator launch-flow teardown; (3) legacy desktop-settings fallback read can be dropped once pre-release workspaces are irrelevant.

## Related Notes

- Step: [[02_Phases/Phase_21_pivot_groundwork_and_aggregator_teardown/Steps/Step_04_bootstrap-workspace-v2-layout-and-contracts-v2-domain-skeleton|STEP-21-04 Bootstrap workspace v2 layout and contracts v2 domain skeleton]]
- Phase: [[02_Phases/Phase_21_pivot_groundwork_and_aggregator_teardown/Phase|Phase 21 pivot groundwork and aggregator teardown]]
