# SRGNT development team: coordinator (glm-5-turbo), researcher (minimax-m2.7), executor (glm-5.1), reviewer (gpt-5.4/codex), tester (minimax-m2.7). Active context management with 50% max utilization. Loop: researcher → executor → reviewer → tester until 100% pass.
srgnt-team:
  - coordinator
  - researcher
  - executor-1
  - reviewer
  - tester

