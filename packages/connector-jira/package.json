{
  "name": "@srgnt/connector-jira",
  "version": "0.1.0",
  "private": true,
  "description": "Jira connector package for srgnt",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@srgnt/connectors": "workspace:*",
    "@srgnt/contracts": "workspace:*",
    "@srgnt/jira-client": "workspace:*"
  },
  "devDependencies": {
    "@srgnt/tsconfig": "workspace:*",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
