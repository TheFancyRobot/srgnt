/**
 * `@srgnt/harness/testing` — the deterministic ACP test substrate reused across
 * phases 23–28: a scriptable mock agent (scenario schema + runner + in-process
 * connector) and a fixture recorder for pinning tolerant-decode behavior.
 */
export * from './mock-agent/index.js';
export * from './fixtures/index.js';
